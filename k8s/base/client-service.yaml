apiVersion: v1
kind: Service
metadata:
  name: react-client
  labels:
    app: react-client
spec:
  type: ClusterIP
  selector:
    app: react-client
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
